type Company {
  uuid: String!
  createdAt: String
  name: String!
  managers(page: Page, filter: ManagerFilter): ManagerPage!
}

type CompanyPage implements PaginatedQuery {
  edges: [Company]
  pageInfo: PageInfo
}
