import { Meta, Story, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from '@storybook/addon-knobs';
import CoreInput, {InputTypes} from './CoreInput';

<Meta title="Core/CoreInput" component={CoreInput}/>

# Description
An input component with minimal styling for use in more complex input forms.
This component handles the basics, placeholders, text changing, removing default styles etc.

# Preview

<Preview>
  <Story name="CoreInput" parameters={{ decorators: [withKnobs] }}>
    <CoreInput
      type={select('Input Type', ['email','number','password','search','tel','text'], 'text')}
      onChange={()=>{}}
      placeholder={text('Placeholder', 'joe@blogs.com')}
    />
  </Story>
</Preview>

# Props

<Props of={CoreInput} />